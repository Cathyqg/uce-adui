# =============================================================================
# Core dependencies - LangGraph & LangChain 1.0 GA (2025-10)
# =============================================================================
langgraph>=1.0.0,<2.0.0
langchain>=1.0.0,<2.0.0
langchain-core>=1.0.0,<2.0.0
langchain-community>=0.3.0  # 社区集成（包含 FAISS 等向量存储）

# =============================================================================
# LLM 提供商 - 支持多种模型
# =============================================================================

# LiteLLM - 统一接口支持 100+ 模型（可选）
litellm>=1.50.0


# =============================================================================
# API Framework
# =============================================================================
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0

# =============================================================================
# Database/Checkpointing
# =============================================================================
# langgraph-checkpoint>=2.0.0
# langgraph-checkpoint-postgres>=2.0.0
# psycopg[binary]>=3.2.0
# redis>=5.2.0

# =============================================================================
# Utilities & Best Practices
# =============================================================================
httpx>=0.27.0
tenacity>=9.0.0  # 重试机制 - Best Practice
# structlog>=24.0.0  # 结构化日志 - Best Practice
# opentelemetry-api>=1.27.0  # 可观测性 - Best Practice
# opentelemetry-sdk>=1.27.0

# =============================================================================
# Tools 依赖 - LangGraph 1.0+ Tools Support
# =============================================================================

# 向量搜索 (用于 BDL 组件智能搜索 - 可选但推荐)
# faiss-cpu>=1.9.0  # Facebook AI Similarity Search (CPU版本)
# faiss-gpu>=1.9.0  # GPU 版本（如果有 CUDA）
# numpy>=1.26.0  # FAISS 依赖

# Embeddings (选择其中一种)
# 方案 2: 使用本地模型
# sentence-transformers>=3.0.0

# =============================================================================
# Development
# =============================================================================
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0  # 覆盖率
black>=24.0.0
isort>=5.13.0
mypy>=1.8.0
ruff>=0.7.0  # 更快的 linter

# =============================================================================
# Tools 相关依赖 - LangGraph 1.0+ Tools Support
# =============================================================================

# 向量搜索 (用于 BDL 组件语义搜索)
# 选择其中一种:

# 方案 1: FAISS (推荐 - Facebook AI Similarity Search)
# faiss-cpu>=1.9.0  # CPU 版本，适合大多数场景
# faiss-gpu>=1.9.0  # GPU 版本，需要 CUDA

# 方案 2: ChromaDB (本地向量数据库)
# chromadb>=0.5.0


# Sentence Transformers (本地 embedding 模型)

# =============================================================================
# Optional: LangSmith for tracing (强烈推荐用于生产)
# =============================================================================
# langsmith>=0.2.0  # 可观测性追踪

# =============================================================================
# Optional: 代码质量工具（如果不依赖外部 npx 命令）
# =============================================================================
# 这些可以通过 Python 绑定调用，而不依赖 Node.js
# py-typescript>=0.1.0  # TypeScript 的 Python 绑定（如果有）

# =============================================================================
# Optional: 视觉对比工具
# =============================================================================
# pillow>=11.0.0  # 图像处理
# playwright>=1.48.0  # 浏览器自动化
# opencv-python>=4.10.0  # 图像对比

# =============================================================================
# Optional: 性能监控
# =============================================================================
# prometheus-client>=0.21.0  # Prometheus 指标
# tiktoken>=0.8.0  # Token 计数
